@model GestionStageEquipe7.Areas.Stages.Models.EmployeurMissionEmployeur

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>EmployeurMissionEmployeur</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="EmployeurMissionEmployeurId" />
            <input type="hidden" asp-for="EmployeurId" value="@ViewBag.EmployeurId" />
            <div class="form-group">
                <label asp-for="MissionEmployeurId" class="control-label"></label>
                <select asp-for="MissionEmployeurId" class="form-control" asp-items="ViewBag.MissionEmployeurId"></select>
                <span asp-validation-for="MissionEmployeurId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
